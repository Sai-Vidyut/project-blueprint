# `prompts`

System/user prompts live here, not inline in `lib/ai` or components. Each prompt should instruct the model to return exactly the `Blueprint` shape (`src/types/blueprint.ts`) as JSON, matching `src/lib/schemas/blueprint.ts`.
